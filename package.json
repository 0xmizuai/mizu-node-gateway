{
  "name": "metadata-migrator",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "start": "wrangler dev --config wrangler.local.toml --local",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "lint": "eslint 'src/**/*.{js,ts}' --quiet",
    "lint:fix": "eslint 'src/**/*.{js,ts}' --quiet --fix",
    "format": "prettier --write \"src/**/*.{js,jsx,ts,tsx,json,css,scss,md}\"",
    "format:check": "prettier --check \"src/**/*.{js,jsx,ts,tsx,json,css,scss,md}\"",
    "pre-commit": "lint-staged",
    "prepare": "husky",
    "type-check": "tsc --noEmit",
    "qstash:list": "ts-node --project scripts/tsconfig.json scripts/qstash-schedule-manager.ts list",
    "qstash:cancel": "ts-node --project scripts/tsconfig.json scripts/qstash-schedule-manager.ts cancel",
    "qstash:schedule-cleanup": "ts-node --project scripts/tsconfig.json scripts/qstash-schedule-manager.ts schedule-cleanup"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "@cloudflare/workers-types": "^4.20250121.0",
    "@types/node": "^22.10.1",
    "@typescript-eslint/eslint-plugin": "^5.59.0",
    "@typescript-eslint/parser": "^5.59.0",
    "dotenv": "^16.4.7",
    "drizzle-kit": "^0.30.5",
    "eslint": "^8.38.0",
    "eslint-config-prettier": "^8.10.0",
    "eslint-plugin-prettier": "^4.2.1",
    "firebase-admin": "^13.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^13.3.0",
    "prettier": "^2.8.8",
    "ts-node": "^10.9.2",
    "tsc-files": "^1.1.4",
    "typescript": "^5.5.2",
    "vitest": "2.0.5",
    "wrangler": "^3.107.3"
  },
  "dependencies": {
    "@telegram-auth/server": "^1.0.4",
    "@upstash/qstash": "^2.7.21",
    "@upstash/redis": "^1.34.4",
    "chanfana": "^2.6.3",
    "cloudflare": "link:@upstash/redis/cloudflare",
    "drizzle-orm": "^0.40.0",
    "hono": "^4.6.18",
    "jose": "^5.9.6",
    "zod": "^3.24.1"
  },
  "lint-staged": {
    "*.{js,ts,json,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
